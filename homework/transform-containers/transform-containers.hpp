#include <deque>
#include <iostream>
#include <list>
#include <map>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string>&
                                                               list,
                                                           std::deque<int>& deque);